apiVersion: v1
kind: Service
metadata:
  name: {{ template "tastewebapi.fullname" . }}
  labels:
    app: {{ template "tastewebapi.name" . }}
    chart: {{ template "tastewebapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "tastewebapi.name" . }}
    release: {{ .Release.Name }}
